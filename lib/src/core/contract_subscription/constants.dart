const kDefaultPollingInterval = Duration(seconds: 10);

const kBackgroundPollingInterval = Duration(seconds: 60);

const kIntensivePollingInterval = Duration(seconds: 2);

const kNextBlockTimeout = Duration(seconds: 60);
